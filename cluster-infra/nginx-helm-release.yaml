apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginx-profesional
  namespace: dev
spec:
  interval: 5m
  chart:
    spec:
      chart: nginx
      version: "15.x.x"
      sourceRef:
        kind: HelmRepository
        name: bitnami-repo
        namespace: flux-system
  values:
    replicaCount: 2
    service:
      type: ClusterIP
